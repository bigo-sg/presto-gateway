requestRouter:
  port: 8080
  name: prestoRouter
  historySize: 1000

dataStore:
  jdbcUrl: jdbc:mysql://127.0.0.1:3306/prestogateway
  user: root
  password: root123
  driver: com.mysql.cj.jdbc.Driver

server:
  applicationConnectors:
    - type: http
      port: 8090
  adminConnectors:
    - type: http
      port: 8091

notifier:
  smtpHost: localhost
  smtpPort: 587
  sender: presto-gw-monitor-noreply@lyft.com
  recipients:
    - prestodev@yourorg.com

modules:
  - com.lyft.data.gateway.ha.module.HaGatewayProviderModule

managedApps:
  - com.lyft.data.gateway.ha.GatewayManagedApp

# Logging settings.
logging:
  # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
  level: INFO

  # Logger-specific levels.
  loggers:
    com.lyft: DEBUG

  appenders:
    - type: console
    - type: file
      currentLogFilename: /Users/bh/data/presto/prestoproxy/prestoproxy-java.log
      archivedLogFilenamePattern: /Users/bh/data/presto/prestoproxy/prestoproxy-java-%d{yyyy-MM-dd}-%i.log.gz
      archivedFileCount: 7
      timeZone: UTC
      maxFileSize: 100MB

authConfig:
  user: admin
  roles: [ADMIN,USER]
  password: password